services:
  - type: web
    name: studygo
    runtime: node
    plan: free
    rootDir: studygo
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
